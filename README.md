# NLP-Assignment-1
# The code is quite simple to run. The main() function handles the chain of events where the model will run and it should print the desired results. Additionally, ensure that the necessary libraries are imported for it to work. As we did, you can use "Dataset" to upload the data.
